<div class="flex flex-col h-screen bg-gray-50">
  <header class="bg-white shadow px-6 py-4 z-10">
    <h1 class="text-xl font-bold text-gray-800 flex items-center gap-2">
      <span>📚</span>
      <%= @chat_session.topic.title %>
    </h1>
    <p class="text-xs text-gray-500 mt-1">
      ID de Sesión: <span class="font-mono"><%= @chat_session.id %></span>
    </p>
  </header>

  <div class="flex-1 overflow-y-auto p-6 space-y-6" id="messages-container" data-controller="scroll">
    <%= turbo_stream_from @chat_session %>
    
    <div id="messages" class="flex flex-col space-y-4">
      <%= render @chat_session.messages %>
    </div>
  </div>

  <footer class="bg-white border-t p-4">
    <div class="max-w-4xl mx-auto">
      <%= render "messages/form", chat_session: @chat_session %>
    </div>
  </footer>
</div>